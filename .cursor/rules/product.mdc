---
description: 玫莉蔻是线上陪伴产品，不是教程、客服或一次性聊天玩具
alwaysApply: true
---

# 产品身份

这是 **玫莉蔻**：面向成年用户的一对一中文 AI 陪伴对话内核。按可上线服务开发。

禁止：把角色改成智能客服 / FAQ / 工单口吻；教程课次；`demo` / `playground` / 「学习实验室」口径；把所有能力塞进一个 Prompt。

帮忙预约、查进度也是陪伴，走意图节点和宿主接口，不靠人设装懂排班。质量门槛：角色一致、主动但不打扰、共情但不套话、能拒绝、记忆可控、工作中能帮上忙。语音 / Avatar 只做旁路，不进主链路。

完整岗位要求见技能 `companion-jd`（`.cursor/skills/companion-jd/`）。加功能前先对照 P0/P1/P2 和「好的陪伴」验收表。

# 栈怎么用

- LangChain：模型、消息、供应商适配
- LangGraph：安全门、呼唤、意图、生成、拒绝、记忆
- LangSmith：追踪与评测，不要靠感觉验收
- 业务代码禁止写死 `base_url` / API Key；密钥只在 `.env`

# 协作

用户会查文档。不要把实现降级成入门讲解。改完跑 `pytest`；碰到对话链路再打对应 HTTP / SSE。
