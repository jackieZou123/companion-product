---
description: 按任务拆分少量多次提交，降低混改与幻觉风险
alwaysApply: true
---

# 增量提交

多步骤任务**优先拆成多个小 commit**，而不是一次提交整坨改动。

## 原则

1. **一 commit 一意图**
2. **可独立回滚**：revert 后其余提交仍应 `pytest` 通过
3. **先拆后写**：大任务先列出切分，再按序实施
4. **提交前自检**：`git diff` 是否混入无关文件

## 何时必须拆分

| 场景 | 建议 |
|------|------|
| 人设 + 安全门 + 前端 | 分开提交 |
| 规则/文档 + 业务代码 | 文档单独 `docs:` |
| 多个独立 bugfix | 每个 bug 一个 `fix` |
| Plan 多步骤已确认 | 每步一个 commit |

单文件、单意图、行数很少时可合并。用户明确要求「合成一次提交」时合并。

## 禁止

- 把人设、安全门、文档、无关重构塞进同一个 commit
- 用户未要求时主动 `git push`
- 用 `--no-verify` 绕过检查
- subject 写「misc」「各种修复」
